public class ArrayProg {
    public static void Prog(){
        
        integer[] numarray = new integer[5];
        numarray[0] = 100;
        numarray[1] = 200;
        numarray[2] = 300;
        numarray[3] = 400;
        numarray[4] = 500;
        System.debug('Numbner Array: '+numarray);
        
        for(integer x:numarray){
            System.debug('array:'+x);
        }
    }
}