<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Paid_data_Consistency</fullName>
    <active>true</active>
    <description>Payment date should be mentioned based on the payment Status</description>
    <errorConditionFormula>OR(
/* payment paid and date not present */
AND(
ISPICKVAL( Payment_Status__c , &apos;Paid&apos;),
ISBLANK( Payment_Date__c )
),
/* payment not paid and date present in system */
AND(
NOT(ISPICKVAL( Payment_Status__c, &apos;Paid&apos;)),
NOT(ISBLANK( Payment_Date__c ))
)
)</errorConditionFormula>
    <errorDisplayField>Payment_Date__c</errorDisplayField>
    <errorMessage>Payment date cannot be blank if payment is paid.</errorMessage>
</ValidationRule>
